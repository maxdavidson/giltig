{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug tests",
      "type": "node",
      "request": "launch",
      "runtimeArgs": ["--test", "--enable-source-maps"],
      "skipFiles": ["<node_internals>/**"],
      "preLaunchTask": "${defaultBuildTask}"
    }
  ]
}
